{"TrailId":"2026-08-05-claude-code-marketplace-setup","Actor":"Planner","CreatedAt":"2026-08-05T01:50:00Z","TrueIntent":"Shawn wants persistent, installable Claude Code commands (like /ceo:evolve-colony) for this repo -- the actual Claude Code plugin-marketplace mechanism, not the existing custom .agents/plugins/marketplace.json (which is a separate, unrelated convention consumed only by MarketplaceSkillsMaterializer.cs in the .NET runtime).","Decision":"Create a real .claude-plugin/marketplace.json at repo root per Anthropic's documented schema (fetched and verified this cycle, not assumed from training data -- schema may have changed). Reuse the existing skills/ directory in place via source:'./' + per-entry 'skills' paths, rather than duplicating or restructuring any skill content -- avoids touching the .NET runtime's existing convention. Build the CEO evolve-colony command as a real commands/ file so /ceo:evolve-colony actually resolves to something.","Plan":["Fetch and verify the current official marketplace.json + plugin-entry schema from code.claude.com docs -- do not assume from memory","Create skills/ceo/commands/evolve-colony.md: the CEO agent-router workflow (inventory -> dependency-resolver check -> classify gap as command/sub-agent/new domain -> report, does not build)","Auto-generate .claude-plugin/marketplace.json by reading every skills/*/SKILL.md frontmatter (name + description), one plugin entry per skill, source:'./' + skills:['./skills/<name>'] + strict:false so no per-plugin plugin.json files are needed","Give the ceo entry a commands field pointing at the new evolve-colony.md","Validate the generated JSON parses and manually inspect for parsing defects (multi-line YAML descriptions, missing frontmatter) rather than trusting the generation script blindly"],"SuccessCriteria":[".claude-plugin/marketplace.json exists, is valid JSON, matches the documented schema (name/owner/plugins required)","Every skills/ subdirectory with a SKILL.md has exactly one corresponding plugin entry","Every entry's description is the real, accurate skill description -- no truncated or garbled text from multi-line YAML parsing","skills/ceo/commands/evolve-colony.md exists and is referenced in the ceo plugin entry's commands field","No existing skills/ content modified or duplicated"]}